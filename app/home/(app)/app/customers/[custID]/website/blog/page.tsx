import React from "react";

const CustomerWebsiteBlog = () => {
  return (
    <div>
      CustomerWebsiteBlog Should also have component for scheduled and
      non-schedule blog articles in queue
    </div>
  );
};

export default CustomerWebsiteBlog;
